<%= render partial: 'book', locals: { book: @book } %>
<%= render partial: 'book_genres', locals: { book: @book } %>
<%= link_to "Back to Products", products_path(product_type: params[:product_type]) %>